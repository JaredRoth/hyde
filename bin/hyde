#!/usr/bin/env ruby

require_relative '../lib/file_handler'
require_relative '../lib/hyde'

argv = ARGV

puts "You ran Hyde with these arguments: #{argv}"

hyde = Hyde.new(ARGV)
hyde.process_subcommand
